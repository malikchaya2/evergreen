tasks:
  - name: this_is_a_task_name
    commands:
      - command: shell.exec
        params:
          script: echo "running task this_is_a_task_name"
  - name: this_is_another_task_name
    commands:
      - command: shell.exec
        params:
          script: echo "running task this_is_another_task_name"
  - name: task3
    commands:
      - command: shell.exec
        params:
          script: echo "running task task3"
  - name: task4
    commands:
      - command: shell.exec
        params:
          script: echo "running task task4"
  - name: task5
    commands:
      - command: shell.exec
        params:
          script: echo "running task task5"
  - name: task6
    commands:
      - command: shell.exec
        params:
          script: echo "running task task6"

task_groups:
  - name: group_few_tasks_teardown_task
    teardown_task:
      - command: shell.exec
        params:
          script: echo "this is the teardown_task"
    setup_task:
      - command: shell.exec
        params:
          script: echo "this is the setup_task"
    tasks:
      - this_is_a_task_name
      - this_is_another_task_name
  - name: group_one_task_teardown_task
    teardown_task:
      - command: shell.exec
        params:
          script: echo "this is the teardown_task"
    setup_task:
      - command: shell.exec
        params:
          script: echo "this is the setup_task"
    tasks:
      - task3
  - name: group_few_tasks_teardown_group
    teardown_group:
      - command: shell.exec
        params:
          script: echo "this is the teardown_task"
    setup_task:
      - command: shell.exec
        params:
          script: echo "this is the setup_task"
    tasks:
      - task4
      - task5
  - name: group_one_task_teardown_group
    teardown_group:
      - command: shell.exec
        params:
          script: echo "this is the teardown_task"
    setup_task:
      - command: shell.exec
        params:
          script: echo "this is the setup_task"
    tasks:
      - task6

buildvariants:
  - name: ubuntu2204
    display_name: Ubuntu 22.04
    run_on:
      - ubuntu2204-small
      - ubuntu2204-large
    tasks:
      - "group_few_tasks_teardown_task"
      - "group_one_task_teardown_task"
      - "group_few_tasks_teardown_group"
      - "group_one_task_teardown_group"
