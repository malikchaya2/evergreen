tasks:
  - name: task4
    commands:
      - command: shell.exec
        params:
          script: echo "running task task4"
  - name: task5
    commands:
      - command: shell.exec
        params:
          script: echo "running task task5"
  - name: task6
    commands:
      - command: shell.exec
        params:
          script: echo "running task task6"

task_groups:
  - name: group_one_task_teardown_group
    teardown_group:
      - command: shell.exec
        params:
          script: echo "this is the teardown_task"
    setup_task:
      - command: shell.exec
        params:
          script: echo "this is the setup_task"
    tasks:
      - task6
  - name: closer_copy
    setup_group_can_fail_task: true
    setup_group_timeout_secs: 1800
    setup_group:
      - command: shell.exec
        params:
          shell: bash
          script: echo "this is the setup_group"
    teardown_group:
      - command: shell.exec
        params:
          shell: bash
          script: echo "this is the teardown_group"
    tasks:
      - task4
buildvariants:
  - name: ubuntu2204
    display_name: Ubuntu 22.04
    run_on:
      - ubuntu2204-small
      - ubuntu2204-large
    tasks:
      - "closer_copy"
