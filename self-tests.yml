tasks:
  - name: task4
    commands:
      - command: shell.exec
        params:
          script: echo "running task task4"
  - name: task5
    commands:
      - command: shell.exec
        params:
          script: echo "running task task5"

buildvariants:
  - name: ubuntu2204
    display_name: Ubuntu 22.04
    run_on:
      - ubuntu2204-small
    tasks:
      - "task4"
      - "task5"
